<div class="ui column card">
  <%= link_to theme_path(theme) do %>
    <h3><%= theme.name %></h3>
    <%= image_tag theme.default_image_url %>
  <% end %>
  <!-- TODO: link to live preview, created_at date -->
  <span class="ui left floated icon price-point"><%= number_to_currency(theme.price_list.single_tier, precision: 0) %></span>
  <a href="#" class="ui right floated labeled icon button">
    preview
    <i class="zoom in icon"></i>
  </a>
</div>