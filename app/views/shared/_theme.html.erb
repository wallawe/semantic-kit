<div class="ui column">
  <h3><%= link_to theme.name, theme_path(theme) %></h3>
  <%= image_tag theme.default_image_url %>
  <!-- TODO: add price, link to live preview, created_at date, actual image -->
  <a href="#" class="ui right floated labeled icon button">
    preview
    <i class="zoom in icon"></i>
  </a>
</div>